import { FilterFlightsPipe } from './filter-flights.pipe';

describe('FilterFlightsPipe', () => {
  it('create an instance', () => {
    const pipe = new FilterFlightsPipe();
    expect(pipe).toBeTruthy();
  });
});
